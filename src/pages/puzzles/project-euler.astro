---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { solvedProblems, PROJECT_EULER_PROBLEMS_TOTAL } from '../../data/project-euler';

const solvedCount = Object.keys(solvedProblems).length;
const pct = Math.round((solvedCount / PROJECT_EULER_PROBLEMS_TOTAL) * 100);

const blockSize = 100;
const fullBlocks = Math.floor(PROJECT_EULER_PROBLEMS_TOTAL / blockSize);
const remainder = PROJECT_EULER_PROBLEMS_TOTAL % blockSize;

const mainBlocks = Array.from({ length: fullBlocks }, (_, b) =>
  Array.from({ length: blockSize }, (_, i) => b * blockSize + i + 1)
);
const lastBlock = remainder > 0
  ? Array.from({ length: remainder }, (_, i) => fullBlocks * blockSize + i + 1)
  : [];
---
<BaseLayout title="Project Euler">
  <p class="breadcrumb"><a href="/puzzles/">← Puzzles</a></p>
  <h1>Project Euler</h1>

  <p><a href="https://projecteuler.net" target="_blank">Project Euler</a> is a series of challenging mathematical and computational problems that require both mathematical insight and programming skill. I enjoy digging into them — my dream is to one day have every single problem solved.</p>

  <div class="profile-wrap">
    <img src="https://projecteuler.net/profile/KrolKubaV.png" alt="KrolKubaV's Project Euler progress" />
  </div>

  <div class="stats">
    <div class="stat-header">
      <span class="stat-label">Solved</span>
      <span class="stat-count">{solvedCount} / {PROJECT_EULER_PROBLEMS_TOTAL} ({pct}%)</span>
    </div>
    <div class="progress-bar">
      <div class="progress-fill" style={`width: ${pct}%`}></div>
    </div>
  </div>

  <div class="blocks">
    {mainBlocks.slice(0, 9).map(block => (
      <div class="block">
        <span class="block-label">{block[0]}–{block[block.length - 1]}</span>
        <div class="grid">
          {block.map(id => (
            <a href={`https://projecteuler.net/problem=${id}`} target="_blank" class={`cell ${id in solvedProblems ? 'solved' : 'unsolved'}`}>
              {String(id).slice(-2)}
            </a>
          ))}
        </div>
      </div>
    ))}
  </div>
  <div class="blocks">
    {mainBlocks.slice(9).map(block => (
      <div class="block">
        <span class="block-label">{block[0]}–{block[block.length - 1]}</span>
        <div class="grid">
          {block.map(id => (
            <a href={`https://projecteuler.net/problem=${id}`} target="_blank" class={`cell ${id in solvedProblems ? 'solved' : 'unsolved'}`}>
              {String(id).slice(-2)}
            </a>
          ))}
        </div>
      </div>
    ))}
    {lastBlock.length > 0 && (
      <div class="block">
        <span class="block-label">{lastBlock[0]}–{lastBlock[lastBlock.length - 1]}</span>
        <div class="grid">
          {lastBlock.map(id => (
            <a href={`https://projecteuler.net/problem=${id}`} target="_blank" class={`cell ${id in solvedProblems ? 'solved' : 'unsolved'}`}>
              {String(id).slice(-2)}
            </a>
          ))}
        </div>
      </div>
    )}
  </div>
</BaseLayout>

<style>
  .breadcrumb {
    margin-bottom: 2rem;
  }
  .profile-wrap {
    margin: 2rem 0 1.5rem;
    text-align: center;
  }
  .profile-wrap img {
    display: block;
    margin: 0 auto;
    image-rendering: pixelated;
  }
  .stats {
    margin: 2rem 0 1.5rem;
  }
  .stat-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 0.5rem;
  }
  .stat-label {
    font-weight: 700;
    font-size: 1rem;
    color: #111827;
  }
  .stat-count {
    font-size: 0.9rem;
    color: #6b7280;
  }
  .progress-bar {
    width: 100%;
    height: 10px;
    background: #f3f4f6;
    border-radius: 5px;
    overflow: hidden;
  }
  .progress-fill {
    height: 100%;
    background: #1e3a5f;
    border-radius: 5px;
    transition: width 1s ease;
  }
  .blocks {
    margin: 1.5rem 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
  }
  .block {
    border: 1px solid #e5e7eb;
    border-radius: 6px;
    padding: 0.4rem 0.5rem 0.5rem;
    flex: 0 0 auto;
  }
  .block-label {
    display: block;
    text-align: center;
    font-size: 0.6rem;
    color: #9ca3af;
    margin-bottom: 0.2rem;
    font-weight: 600;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(10, 1fr);
    gap: 1px;
  }
  .cell {
    aspect-ratio: 1;
    border-radius: 1px;
    font-size: 0.45rem;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    min-width: 8px;
  }
  .cell:hover {
    opacity: 0.6;
    text-decoration: none;
  }
  .solved {
    background: #1e3a5f;
    color: rgba(255,255,255,0.85);
  }
  .unsolved {
    background: #f3f4f6;
    color: transparent;
  }
</style>
