---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostItem from '../../components/PostItem.astro';
import { getCollection } from 'astro:content';
import { SECTIONS } from '../../config';

const section = SECTIONS['puzzles'];
const posts = (await getCollection('puzzles'))
  .filter(p => !p.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const subsections = section.subsections.map(sub => ({
  ...sub,
  posts: posts.filter(p => p.data.subsection === sub.slug),
}));

const ungrouped = posts.filter(p => !p.data.subsection || !section.subsections.some(s => s.slug === p.data.subsection));
---
<BaseLayout title="Puzzles">
  <h1>Puzzles</h1>

  {subsections.map(sub => (
    <section>
      <h2><a href={`/my-blog/puzzles/${sub.slug}/`}>{sub.label}</a></h2>
      {sub.posts.length > 0 ? (
        <ul>
          {sub.posts.map(post => (
            <PostItem
              title={post.data.title}
              pubDate={post.data.pubDate}
              slug={`/my-blog/puzzles/read/${post.id}`}
              description={post.data.description}
            />
          ))}
        </ul>
      ) : (
        <p style="color: var(--color-muted);">No posts yet.</p>
      )}
    </section>
  ))}

  {ungrouped.length > 0 && (
    <section>
      <h2>Other</h2>
      <ul>
        {ungrouped.map(post => (
          <PostItem
            title={post.data.title}
            pubDate={post.data.pubDate}
            slug={`/puzzles/read/${post.id}`}
            description={post.data.description}
          />
        ))}
      </ul>
    </section>
  )}
</BaseLayout>
